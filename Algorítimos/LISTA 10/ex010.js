function analisar() {
  res.innerHTML = ``;
  let salto = Number(document.getElementById("dias").value);
  let c = 1;
  let i = 1;


  while ((c < 31, i < 31)) {
    
    res.innerHTML += `<br>Dia ${i} - <b>Musculação</b>`;
  }
}
