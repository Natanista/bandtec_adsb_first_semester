function raizQuadrada() {
  let raiz2 = Number(numero.value) * Number(numero.value);
  resultado.innerHTML = `${raiz2}`;
}

function raizCubica() {
  let raiz3 =
    Number(numero.value) * Number(numero.value) * Number(numero.value);
  resultado.innerHTML = `${raiz3}`;
}
